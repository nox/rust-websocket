initSidebarItems({"enum":[["Opcode","Represents a WebSocket data frame opcode"]],"struct":[["DataFrame","Represents a WebSocket data frame.The data held in a DataFrame is never masked. Masking/unmasking is done when sending and receiving the data frame,This DataFrame, unlike the standard Message implementation (which also implements the DataFrame trait), owns its entire payload. This means that calls to `payload` don't allocate extra memory (again unlike the default Message implementation)."]]});