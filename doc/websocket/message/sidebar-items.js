initSidebarItems({"enum":[["Type","Valid types of messages (in the default implementation)"]],"struct":[["Message","Represents a WebSocket message.This message also has the ability to not own its payload, and stores its entire payload in chunks that get written in order when the message gets sent. This makes the `write_payload` allocate less memory than the `payload` method (which creates a new buffer every time).Incidentally this (the default implementation of Message) implements the DataFrame trait because this message just gets sent as one single DataFrame."]],"trait":[["IntoCowBytes","Trait representing the ability to convert self to a `Cow<'a, [u8]>`"]]});